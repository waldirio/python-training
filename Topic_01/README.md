Will be covered on this topic ...

- Virtual Environment
```
Virtual Environment
  Using 2.7 *for a while*
    pip install virtualenv
    virtualenv -p python ~/.virtualenvs/example01 
  Virtual env folder located in ~/.virtualenvs/example01
  Using Python 3.6
  Check all modules installed
    pip list
  Install ipython
    pip install ipython
```
- Play around with strings
```
Play around with strings
  aux1="ola"
  dir(aux1)
    Here we will see count, upper, lower, isspace
  help(aux1.???)
    What expect as parameter and/or as result
  aux2="Rio de Janeiro"
```

- Play around with lists
```
Play around with lists
  local_list = []
  Check the list Content
    local_list
  Add the content of aux1 on the list local_list
    local_list.append(aux1)
  Add the content of aux2 on the list local_list
    local_list.append(aux2)
  Methods available on the list
    dir(local_list)
  Let's order, count, remove and do interesting stuff with our list
    local_list.<???>
```
